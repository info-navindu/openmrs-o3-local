services:
  gateway:
    image: openmrs/openmrs-reference-application-3-gateway:${TAG:-qa}
    restart: "unless-stopped"
    depends_on:
      - frontend
      - backend
    ports:
      - "80:80"

  frontend:
    image: openmrs/openmrs-reference-application-3-frontend:${TAG:-qa}
    restart: "unless-stopped"
    environment:
      SPA_PATH: /openmrs/spa
      API_URL: /openmrs
      SPA_CONFIG_URLS: /openmrs/spa/config-core_demo.json
      SPA_DEFAULT_LOCALE:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/"]
      timeout: 5s
    depends_on:
      - backend

  backend:
    image: openmrs/openmrs-reference-application-3-backend:${TAG:-qa}
    restart: "unless-stopped"
    depends_on:
      - db
    environment:
      OMRS_CONFIG_MODULE_WEB_ADMIN: "true"
      OMRS_CONFIG_AUTO_UPDATE_DATABASE: "true"
      OMRS_CONFIG_CREATE_TABLES: "true"
      OMRS_CONFIG_CONNECTION_SERVER: db
      OMRS_CONFIG_CONNECTION_DATABASE: openmrs
      OMRS_CONFIG_CONNECTION_USERNAME: ${OMRS_DB_USER:-openmrs}
      OMRS_CONFIG_CONNECTION_PASSWORD: ${OMRS_DB_PASSWORD:-openmrs}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/openmrs"]
      timeout: 5s
    volumes:
      - openmrs-data:/openmrs/data

  # MariaDB
  db:
    image: mariadb:10.11.7
    restart: "unless-stopped"
    command: "mysqld --character-set-server=utf8mb4 --collation-server=utf8mb4_general_ci"
    healthcheck:
      test: "mysql --user=${OMRS_DB_USER:-openmrs} --password=${OMRS_DB_PASSWORD:-openmrs} --execute \"SHOW DATABASES;\""
      interval: 3s
      timeout: 1s
      retries: 5
    environment:
      MYSQL_DATABASE: openmrs
      MYSQL_USER: ${OMRS_DB_USER:-openmrs}
      MYSQL_PASSWORD: ${OMRS_DB_PASSWORD:-openmrs}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-openmrs}
    volumes:
      - db-data:/var/lib/mysql

  # OpenELIS - Commented out, using official deployment separately
  # See: openmrs-o3-local/OpenELIS-Global-2/ for the official deployment
  #
  # openelis-db:
  #   image: postgres:10
  #   container_name: hmis-lab-db
  #   restart: "unless-stopped"
  #   environment:
  #     - POSTGRES_USER=clinlims
  #     - POSTGRES_PASSWORD=clinlims
  #     - POSTGRES_DB=clinlims
  #   volumes:
  #     - openelis-db-data:/var/lib/postgresql/data
  #     - ./init-lab-db.sql:/docker-entrypoint-initdb.d/init.sql
  #
  # openelis:
  #   image: itechuw/openelis-global-2:develop
  #   container_name: hmis-lab
  #   restart: "unless-stopped"
  #   ports:
  #     - "8082:8080"
  #   depends_on:
  #     - openelis-db
  #   environment:
  #     - spring.datasource.url=jdbc:postgresql://hmis-lab-db:5432/clinlims
  #     - spring.datasource.username=clinlims
  #     - spring.datasource.password=clinlims

  # PACS/Orthanc - Moved to separate deployment
  # See: openmrs-o3-local/orthanc-pacs/ for the standalone deployment
  #
  # pacs:
  #   image: jodogne/orthanc-plugins
  #   container_name: hmis-pacs
  #   restart: "unless-stopped"
  #   ports:
  #     - "4242:4242"
  #     - "8042:8042"
  #   environment:
  #     ORTHANC__NAME: "GSLFH_PACS"
  #     ORTHANC__REGISTERED_USERS: '{"orthanc": "orthanc"}'
  #   volumes:
  #     - orthanc-storage:/var/lib/orthanc/db

volumes:
  openmrs-data: ~
  db-data: ~
  # orthanc-storage: ~  # Moved to separate PACS deployment
  # openelis-db-data: ~  # Moved to separate OpenELIS deployment

networks:
  default:
    name: hmis-network
    external: true
